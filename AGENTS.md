# Agent guidelines

## What this repo is

This repository contains the TypeScript/Deno implementation of Linked Markdown.

## How to work here

- Use `deno.json` as the source of truth for tasks, imports, and package shape.
- Run `deno task check` for type and validation work.
- Run `deno task test` for behavior changes.
- Keep exported API changes reflected in README examples.
